/*console.log(window.);*/

/*
// 버튼을 상수로 지정
const olBtn = document.getElementById('ol-btn');

// 목록을 상수로 지정
const olList = document.getElementById('ol-list');

// 버튼을 클릭하면 목록에 li element가 추가되는 이벤트

function addList() {
  const addLi = document.createElement('li');
  addLi.innerHTML = '새로운 목록';
  olList.appendChild(addLi);

}

olBtn.addEventListener('click', addList);


// ol2
const inputText = document.getElementById('input-text');
const inputSubmit = document.getElementById('input-submit');
const inputForm = document.getElementById('input-form');
const olList2 = document.getElementById('ol-list2');

// inputSubmit을 클릭하면 inputText의 값을 목록에 추가하는 이벤트
inputForm.addEventListener('submit', function(e) {
  e.preventDefault();
  const addLi = document.createElement('li');
  addLi.innerHTML = inputText.value;
  olList2.appendChild(addLi);
  console.log(addLi); 
  inputText.value = '';
})
*/

